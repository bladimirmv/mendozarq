import { CountVisitaPipe } from './count-visita.pipe';

describe('CountVisitaPipe', () => {
  it('create an instance', () => {
    const pipe = new CountVisitaPipe();
    expect(pipe).toBeTruthy();
  });
});
