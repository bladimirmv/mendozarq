import { StatePersonalPipe } from './state-personal.pipe';

describe('StatePersonalPipe', () => {
  it('create an instance', () => {
    const pipe = new StatePersonalPipe();
    expect(pipe).toBeTruthy();
  });
});
