import { ChipColorPipe } from './chip-color.pipe';

describe('ChipColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ChipColorPipe();
    expect(pipe).toBeTruthy();
  });
});
