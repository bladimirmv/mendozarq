const socketConfig: {
  url: string;
  options: {}
} = { url: 'http://localhost:3000', options: {} };

export const environment = {
  production: true,
  API_URL: 'http://localhost:3000',
  socketConfig
};
